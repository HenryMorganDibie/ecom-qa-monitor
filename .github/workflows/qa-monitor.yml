name: Ecom QA Monitor

on:
  push:
    branches: [main]

  schedule:
    - cron: "*/30 * * * *"

  workflow_dispatch:

jobs:
  qa-monitor:
    name: Run Health Checks
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      # ✅ FIXED: safe JSON write
      - name: Write config from secret
        run: |
          printf '%s' "${MONITOR_CONFIG}" > config.json
        env:
          MONITOR_CONFIG: ${{ secrets.MONITOR_CONFIG }}

      # optional but safer validation
      - name: Validate config
        run: |
          echo "Validating config.json..."
          cat config.json
          node -e "JSON.parse(require('fs').readFileSync('config.json','utf8')); console.log('JSON valid ✔')"

      - name: Run QA monitor
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
        run: node src/runner.js

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const { context } = require('@actions/github');
            core.warning(
              `🚨 QA Monitor detected failures at ${new Date().toISOString()}. Check run logs: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            );

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - run: npm test -- --coverage

      - uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7